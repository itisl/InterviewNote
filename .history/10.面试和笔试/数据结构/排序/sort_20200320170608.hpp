#ifndef _SORT_HPP_
#define _SORT_HPP_

#include <bits/stdc++.h>
using namespace std;

namespace Sort {
	
	// 冒泡排序
	vector<int> bubble_sort(const vector<int>& vec){
		int n = vec.size();
		auto arr1 = vec;
		for (int i = 0; i <n-1 ; i--)
		{
			bool flag = false;
			for(int j=)
		}
		return 
	}

	// 选择排序
	void selection_sort(){}

	// 插入排序


	// 


}  // namespace Sort

#endif  // !_SORT_HPP_